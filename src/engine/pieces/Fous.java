package engine.pieces;

import chess.PieceType;
import chess.PlayerColor;

public class Fous extends Pieces {
    public Fous(PlayerColor couleur, PieceType type, int distance) {
        super(couleur, type, distance);
    }
}
